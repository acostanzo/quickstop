# Sample Project

## Install

```bash
pip install sample-project
```

A few example links to nowhere — see [the missing tutorial](tutorial.md),
the [missing api](api.md), the [missing changelog](changelog.md),
and the [missing roadmap](roadmap.md).
