# Overview

`sample-project` provides a single function, `mean`, which
computes the arithmetic mean of a sequence of numbers. The
project is intentionally minimal so the fixture stays focused on
the documentation calibration rather than implementation depth.

See [tutorial](tutorial.md) for the worked example, [api](api.md)
for the reference, and [usage](usage.md) for command-line usage.
