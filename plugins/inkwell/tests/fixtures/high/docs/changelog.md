# Changelog

## v1.0.0 — frozen API

Initial stable release. The `mean` function and the
`Aggregator` class are stable. See [api](api.md) for the formal
contract.

## v0.x — pre-stable

Pre-stable iterations. Internal-only.
