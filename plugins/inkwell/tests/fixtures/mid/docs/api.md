# API Reference

## `mean(values)`

Compute the arithmetic mean of a sequence of numbers.

## `Aggregator`

A streaming accumulator. Methods: `add(value)`, `mean()`,
`reset()`. See [tutorial](tutorial.md) for an example and
[migration](migration.md) for v1 → v2 notes.
