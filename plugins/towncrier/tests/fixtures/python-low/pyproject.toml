[project]
name = "fixture-python-low"
version = "0.0.0"
description = "Towncrier python-low calibration fixture (bait-and-switch case)."
dependencies = [
  "structlog>=23.0",
  "prometheus-client>=0.20",
  "opentelemetry-api>=1.20",
  "fastapi>=0.110",
]
