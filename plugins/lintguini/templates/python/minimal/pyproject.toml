# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Minimal band — presence-only. Formatter wired; linter omits the lint
# select so ruff runs against its built-in defaults.

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.format]
quote-style = "double"
