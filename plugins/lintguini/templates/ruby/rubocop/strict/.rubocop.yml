# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Strict band — rubocop with all five canonical departments enabled
# (Style, Layout, Lint, Metrics, Naming). Matches the rubric's strict
# baseline of 5 cop departments configured.

AllCops:
  NewCops: enable
  TargetRubyVersion: 3.3

Style/FrozenStringLiteralComment:
  Enabled: true

Layout/LineLength:
  Max: 120

Lint/MissingSuper:
  Enabled: true

Metrics/MethodLength:
  Max: 20

Naming/AccessorMethodName:
  Enabled: true
