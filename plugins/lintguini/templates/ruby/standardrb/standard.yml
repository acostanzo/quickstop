# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Band-agnostic — standardrb's design rejects rule-by-rule debates, so
# strict / lenient / minimal collapse onto a single config. The flag
# the configure skill picks is "standardrb"; the strictness band picks
# the rubocop path instead if a band variation is wanted.

ruby_version: 3.3
fix: true
