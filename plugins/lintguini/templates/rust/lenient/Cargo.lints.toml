# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Lenient band — Cargo.toml fragment. Forbids unsafe; clippy at default
# severity (warn) for the all-group rather than pedantic.

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
