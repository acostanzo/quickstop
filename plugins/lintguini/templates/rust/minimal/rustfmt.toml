# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Minimal band — formatter presence only. Linter relies on clippy's
# built-in defaults (no [lints] block written).

edition = "2021"
