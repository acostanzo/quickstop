# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Strict band — Cargo.toml fragment. Merge under [lints] in the consumer's
# Cargo.toml; do not drop this file in as a standalone Cargo.toml.

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
pedantic = "warn"
