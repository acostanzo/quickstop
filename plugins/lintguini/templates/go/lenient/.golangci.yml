# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Lenient band — the three highest-signal linters from the strict set
# (errcheck, govet, staticcheck). Quieter than strict; same shape.

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
