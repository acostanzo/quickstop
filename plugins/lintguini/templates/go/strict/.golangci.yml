# Generated by lintguini — see plugins/pronto/references/roll-your-own/lint-posture.md for the source of truth.
# Strict band — full rubric baseline (6 linters: errcheck, gosimple,
# govet, ineffassign, staticcheck, unused). gofmt runs separately.

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
