# Pulse — 2026-04-26

## 19:35

Phase 1 follow-up branch open. `phase-1-avanti.md` promoted `active → done` (PR #41 merged 2026-04-21 with all 12 T-tickets + 3 A-bars closed and acceptance passing). Drafted `phase-1-followup-avanti.md` to scope the post-Phase-1 alignment work that surfaced after pronto Phase 2 PR H1 (compatible_pronto enforcement) and ADR-005 (sibling skill conventions) landed on main.

## 19:40

T1 of the follow-up plan landed: avanti now declares `compatible_pronto: ">=0.1.0"`, version bumped 0.1.2 → 0.1.3 across plugin.json + marketplace.json + root README, audit SKILL field names aligned to pronto's wire contract (`level → severity`, `path → file`, `action → title`, `rationale` decomposed into `command` + `category` + `impact_points`). `audit_ignore: true` overrides now also surface as JSON `info`-severity findings, not just markdown. A3 step 3 deferred at PR-41 time closed via structural verification: pronto's project-record dispatch trace ends at `source: sibling` with `branch: in_range` from the handshake helper. Latent ticket-skill ID-collision bug noted in the closed ticket for a separate follow-up.

## 19:55

Read-through pass after the T1 commit caught three things the JSON-shape grep missed: (1) the markdown rendering example for recommendations still used `<action>`/`<rationale>` placeholders — fixed to `<title>` / `(+<impact_points> pts <category>)` / `<command>`; (2) Step 4b didn't say explicitly that `info` findings don't deduct from the score — added; (3) `audit-thresholds.md` referred to "per finding level" instead of "per finding severity" — aligned. Plan + ticket acceptance grep tightened to cover both JSON keys and markdown placeholders. Lesson: the next time I write a contract-alignment grep, include both `"<key>":` and bare-placeholder forms.

## 20:30

Live A3 step 3 ran via `claude --print --plugin-dir <pronto> --plugin-dir <avanti> --max-budget-usd 5 /pronto:audit --json` against a fresh fixture repo. Result: `dimension: project-record` lands at `source: sibling`, `source_plugin: avanti`, `weighted_contribution: 5.0`, `score: 100`, with avanti's full envelope embedded as `source_audit`. Avanti was the only sibling on the run dispatched **without** a `compatible_pronto`-missing soft note (claudit/skillet/commventional all carried one). The wire-contract field alignment from the T1 commit is what made `source_audit` embed cleanly. Lesson: the "needs interactive Claude Code session" framing was overcautious — `claude --print` orchestrated by Bash is exactly the right primitive for live integration tests in a batch agent. Fixture and stdout/stderr captures cleaned up after verification.

## 21:30

Self-review pass on PR #53 surfaced four substantive issues; pushed back on each. Net changes: tightened `compatible_pronto` from `>=0.1.0` to `>=0.2.0 <0.3.0` (pre-1.0 idiomatic; floor matches the version actually tested; pinned-minor ceiling forces deliberate re-validation when pronto H3 ships, aligning with the natural observations[]-migration timing); amended the plan's stale "out of scope" bullet on live integration since the third commit already executed it; bundled the latent ticket-skill ID-collision bug fix (skills/ticket/SKILL.md Step 3 now scopes the per-id collision check to PLAN_SLUG, leaves slug guard repo-wide). Two surprise findings from re-running the live integration with the tightened range: (a) pronto's discovery prefers `~/.claude/plugins/cache/quickstop/<sibling>/<v>` over `--plugin-dir`, so worktree-side handshake changes can't be exercised end-to-end without syncing the cache (worth a pronto follow-up); (b) when the cache was synced manually, the orchestrator emitted empty stdout — a data point for pronto Phase 2 PR H2b's eval target. Both filed as side-observations in the ticket; neither blocks this PR. Lesson: review interrogation that doesn't push back on the reviewer's own findings is a rubber-stamp.


## 22:00

PR #53 merged via `gh pr merge --rebase --delete-branch` after the test-then-review loop closed with explicit approval (round 3 found no further blockers). Five commits landed on main: the original work + three review-driven iterations + a final plan-staleness sweep. Now promoting `phase-1-followup-avanti.md` from `plans/active/` to `plans/done/` per the plan's DoD — the single ticket (t1) is closed, A3 step 3 verified live, no further work scoped under this plan.
