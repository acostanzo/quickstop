# Pulse — 2026-04-21

## 18:55 — T1 closed

Scaffolded `plugins/pronto/` v0.1.0 skeleton: `plugin.json` (valid, portable, empty `pronto.audits` block), `README.md` (overview + command table + architecture — extends in T2 with rubric/contract links), empty `skills/`, `agents/`, `references/` dirs for subsequent tickets to populate. Registered in `.claude-plugin/marketplace.json` and root `README.md` matching sibling format. Deviation noted in ticket: smith is AskUserQuestion-interactive so the skeleton was hand-authored against smith's template — same shape, author block aligned to sibling convention (`"quickstop"` not `"Anthony Costanzo"`) for the DoD grep check.

Acceptance: plugin.json + marketplace.json both parse as valid JSON; author-string grep returns zero matches inside `plugins/pronto/`.

## 19:10 — T2 closed

Landed `references/rubric.md` and `references/sibling-audit-contract.md`. Rubric lists all 8 dimensions (claude-code-config, skills-quality, commit-hygiene, code-documentation, lint-posture, event-emission, agents-md, project-record) with slugs, weights (sum = 100, verified), owners, presence checks, and letter-grade bands matching claudit. Contract doc defines the `plugin.json` declaration block, stdout JSON shape (top-level + `categories[]` + `findings[]` + `recommendations[]`), validation semantics, and the Phase 1 parser pattern for glue until siblings adopt native support. README extended with links to both references.

Decisions worth recording: cap value for presence-only scores is 50 (tuning knob documented); parser agents live at `agents/parsers/<sibling>.md` (T4 populates); presence checks are coarse-grained by design (depth is every sibling's job).

## 19:35 — T3 closed

Landed `skills/kernel-check/SKILL.md`. Seven checks (AGENTS.md, project-record container, .pronto tool-state, .claude/, README, LICENSE, .gitignore), each binary 0/100. Single JSON emission per run with `plugin: "pronto-kernel"`, `dimension: "kernel"`, categories-based. Orchestrator (T4) reads per-category scores and applies rubric weights + presence cap itself — the kernel stays dumb.

Informal acceptance: exercised the Bash scan against three fixtures (bare repo, simulated pronto-init'd repo, current quickstop-pronto worktree) — arithmetic matches (0 / 100 / 55 composite respectively; the middle fixture's score is exact validation of the "all pass" path, the third is a real mid-execution snapshot). The full three-fixture acceptance as a unit test happens during A1 once `/pronto:init` (T6) exists to scaffold fixture 2 reproducibly.

Scope decision: kernel-check does filesystem-existence only. Semantic presence (skills-quality SKILL.md scan, commit-hygiene regex, lint-config detection, event-emission grep) is orchestrator-owned in T4 — keeps the kernel skill simple and rubric semantics in one place.

## 19:45 — T9 closed

Landed `references/recommendations.json`. 8/8 rubric dimensions covered (script-validated), each with dimension/label/plugin/status/install/audit/parser/roll-your-own/presence-check fields. Plugin status is controlled vocabulary (`shipped` / `phase-1b` / `phase-2-plus`) — `/pronto:init` and `/pronto:improve` both read this to decide what to propose.

Decision: registry is a data file (JSON) not a markdown doc — it's consumed by skills, so machine-parseability wins over prose. Every dimension has a `roll_your_own_ref` populated regardless of sibling status; the Phase 2+ dimensions' install_command is null because their siblings don't exist yet, not because they lack a roll-your-own path.

Proceeded T9 before T4/T6/T8 because those all consume this data — landing it first lets subsequent orchestrator skills reference it as a static file rather than inlining the map.

## 19:55 — T11 closed

Landed `references/report-format.md`. Two surfaces: markdown scorecard (human-default, weakest-first ordering, source-marker visual conventions `✓`/`⊘`/`×`, kernel-health strip footer) and JSON composite (`--json`, schema_version:1, top-level `dimensions[]` each carrying a `source` enum, optional embedded `source_audit` for drill-down, `sibling_integration_notes[]` for partial-failure surfacing).

Source enum has four values: `sibling` (real depth), `kernel-presence-cap` (sibling missing, presence passed, 50 cap), `presence-fail` (sibling missing, presence failed, 0), `kernel-owned` (always kernel-driven, e.g. agents-md). Distinct enums because each has different semantics for `/pronto:improve` — it shouldn't propose an install for `kernel-owned` dimensions.

Decision: markdown is a summary, JSON is the drill-down. The markdown report intentionally omits per-finding detail; everything granular lives in `dimensions[].source_audit`. Keeps the scorecard skimmable (< 30 rows) and the JSON self-contained (< 12 KB target for an 8-dimension audit).

## 20:10 — T5 closed

Landed `templates/` tree: `AGENTS.md`, `project/README.md` + `plans/tickets/adrs/pulse/.gitkeep`, `.claude/README.md`, `.pronto/state.json`, `gitignore-additions.txt`, and the templates manifest `README.md`. Ten files total, all portable (author-grep clean), `state.json` JSON-valid.

Design choices: literal dotfile names in the template tree (avoids rename indirection); `gitignore-additions.txt` appends rather than overwrites (consumers with existing .gitignores don't lose content); `.gitkeep` placeholders for empty subdirs (functional, not documentation). The templates manifest in `templates/README.md` pins the source → target rename map and per-path overwrite strategy (refuse/skip/append) so T6 has a precise contract.

The AGENTS.md template intentionally carries a small repo-specific-conventions block with portable practices as defaults (test-then-review, don't-delegate-understanding) — consumer edits the block in place. project/README.md duplicates some avanti docs on purpose: when avanti isn't installed, that README is the only lifecycle-conventions reference the consumer has.

## 20:35 — T10 closed

Landed `references/roll-your-own/` with eight dimension-specific how-to-do-this-by-hand docs (690 lines total across 8 files, max 115, min 64 — all comfortably under the 200-line target). Consistent shape: "what good looks like" → "minimum viable setup" → "periodic audit checklist" → "common anti-patterns" → "presence check pronto uses" → "concrete first step."

Decisions: vendor-neutral tool recommendations throughout (Biome / Ruff / Pino / OpenTelemetry / Lefthook as open-source defaults); Phase 2+ dimensions get full treatment now even though their sibling plugins don't exist — the conventions outlive the tools. The `agents-md` doc explicitly frames pronto as its own recommendation while still describing the hand-authoring path, since pronto owns that dimension and the roll-your-own framing has to accommodate users who'd rather not run `/pronto:init`.

recommendations.json's `roll_your_own_ref` paths are already aligned: every dimension points at its doc.

## 21:00 — T4 closed

Largest Phase 1 ticket. Four artifacts landed:

- `skills/audit/SKILL.md` — the `/pronto:audit` orchestrator. Seven phases: resolve env → load registries → discover siblings → kernel-check → per-dim scoring → aggregate → emit + persist state. Per-dimension decision tree covers all four source enums from report-format.md.
- `agents/parsers/claudit.md` — 6 categories (Over-Engineering / CLAUDE.md Quality / Security Posture / MCP Config / Plugin Health / Context Efficiency), weights 20/20/15/15/15/15.
- `agents/parsers/skillet.md` — 6 categories (Frontmatter / Instruction Quality / Agent Design / Directory Structure / Over-Engineering / Reference & Tooling), weights 20/20/15/15/15/15.
- `agents/parsers/commventional.md` — 3 categories (Conventional Commits 0.50 / Engineering Ownership 0.30 / Conventional Comments 0.20).

Key design decision: **parsers read repo state directly** — not "parse sibling stdout." None of claudit/skillet/commventional currently ships `--json` or a plugin.json declaration, so the Phase 1 parsers are functional reimplementations of narrow audit slices. Glue. When siblings adopt the contract natively, pronto's discovery picks them up and these parsers are removed in a minor version bump.

Orchestrator stays **read-mostly** — writes only `${REPO_ROOT}/.pronto/state.json` for `/pronto:status`/`/pronto:improve` consumption. No fix application in the audit surface. Parser dispatch uses `pronto:parse-claudit` / `pronto:parse-skillet` / `pronto:parse-commventional` subagent names (plugin-namespaced). Model is haiku — narrow, deterministic, cheap.

Semantic presence fallbacks (skills glob, commit regex, lint-config glob, event-emission grep) are orchestrator-owned per T3's scope decision — kernel-check stays filesystem-only.

## 21:20 — T6 closed

Landed `skills/init/SKILL.md`. Six phases: env → collision scan → plan presentation → copy templates (per-path collision rules: `refuse` for AGENTS.md/project/.pronto, `skip` for .claude/, `append-and-dedupe` for gitignore) → sibling recommendations (filters out phase-2-plus statuses — those plugins don't exist to install yet) → propose installs via AskUserQuestion → summary.

Key decisions: plan-then-act discipline (always show the collision plan before writing); `.claude/` is skip-on-collision rather than refuse (consumers often arrive with existing content); installs run via AskUserQuestion not programmatically (keeps Claude Code's install path in the loop); kernel scaffolding never depends on sibling presence (minimum-viable state is coherent without siblings).

`--force` is coarse — no per-path force flag. Idempotent re-run reports zero new files + `.gitignore already current`.

## 21:35 — T7 closed

Landed `skills/status/SKILL.md`. Read-only — reads `${REPO_ROOT}/.pronto/state.json` and discovers siblings the same way `/pronto:audit` does. Two-line default summary (composite + grade + relative time + counts + next-step hint), `--verbose` shows per-dimension table + per-sibling install-status check.

Decisions: read-only always (no auto-repair — don't hide drift); relative time computed at render (not baked into state — avoids staleness confusion across sessions); next-step hint is a three-way decision rule driven by state (below-threshold → `/pronto:improve`; fresh-installed siblings → `/pronto:audit` to re-pick-up; otherwise → "up to date"). "Fresh-installed" concept surfaces explicitly so a user who just installed a sibling knows their state is stale until they re-audit.

Performance target: under 1s, pure file reads.

## 21:50 — T8 closed

Landed `skills/improve/SKILL.md`. Six phases: env → load state → load registries → rank dimensions (score ascending, ties by descending weight, filtered to `<75`, capped top 5 per session) → walk interactively (per-dim AskUserQuestion with install / walk-roll-your-own / skip / stop options) → append pulse → final summary.

Key decisions: B-threshold filter at score<75 (the plan's "weakest-first" walk implicitly means the weak ones); top-5 cap per session (long walks exhaust attention — remaining dims surface next session via the pulse entry); improve is advisory not state-mutating (only `/pronto:audit` writes state — keeps the read/write contracts clean across skills); never install programmatically (consistent with init — pronto proposes, Claude Code's flow runs with the user in the loop).

Roll-your-own surface: improve reads `references/roll-your-own/<slug>.md` and walks the "Concrete first step" + "Minimum viable setup" sections with the user — doesn't auto-apply. The roll-your-own docs are written with exactly this consumption pattern in mind.

All four user-invocable skills now ship: /pronto:init, /pronto:audit, /pronto:status, /pronto:improve. Plus the internal /pronto:kernel-check invoked by audit.

## 22:05 — T12 closed

Wired research integration: added `Phase 2.5: Load expert context (optional)` to `skills/audit/SKILL.md` and authored `references/research-integration.md` documenting the cache-consumption protocol.

Decisions: ecosystem domain only (sibling-delegated rubric doesn't need core-config or optimization); no pronto-side research agents (when claudit is absent, pronto degrades to deterministic-parser-only scoring — don't duplicate claudit's research surface); invalidation deferred entirely to claudit's three-check scheme (less code, less duplication, less drift). EXPERT_CONTEXT is passed to parsers as supplement, not source of truth — scoring weights stay in pronto's rubric; context informs finding-quality (parsers reference specific best-practices by name in `findings[].message`).

First-run cold-cache cost (10–30s for claudit's research-ecosystem agent) is documented as a one-time cost, not absorbed silently. Every subsequent run across pronto/claudit/skillet/smith/hone is a cache-hit until TTL or version triggers invalidation.

12/12 T-tickets now complete. Remaining: ADR 001 (DoD), A1/A2/A3 acceptance bars, final grep + PR.

## 22:15 — ADR 001 authored and accepted

Landed `project/adrs/001-meta-orchestrator-model.md`. Status: accepted (this plan IS the acceptance). Records the pivot from self-contained template to meta-orchestrator.

Four-part structure per avanti conventions: Context (what was tried — 4 PRs in `acostanzo/pronto` on a bundled-template model; structural problem was every dimension pronto tried to own was already owned by a sibling) → Decision (own the rubric not the depth; delegate per-dimension to siblings; kernel minimal; pronto is a plugin in quickstop, not a separate repo) → Consequences (positive: single source of truth per dimension, consumers install piecemeal, rubric tunable, dogfooding is honest; accepted tradeoffs: pronto useless without siblings, wire contract is upstream work, not-yet-shipped siblings permanently presence-capped until they ship) → Alternatives (keep template, absorb siblings, standalone repo, CI-only — all rejected with reasoning).

Links to phase-1-pronto plan and the parallel phase-1-avanti plan for the Phase 1b workstream.

## 22:30 — A1 / A2 / A3 dry-run acceptance

A-bars exercise user-interactive paths that cannot run autonomously inside this session (AskUserQuestion in `/pronto:init` and `/pronto:improve`; parser subagent dispatch in `/pronto:audit`). Completed dry-run validation instead: Bash-level scans, hand-arithmetic on rubric composites, logic-level verification that every `source` enum has a handler.

- **A1 — fresh-repo bootstrap:** simulated `/pronto:init` via cp'ing templates into a bare repo; ran kernel-check Bash scan (0.004s, well under the 5s budget); hand-computed rubric composite = 25/F for a freshly-scaffolded zero-siblings repo. Scorecard shape: every dimension has a score + source + recommendation; no tracebacks.
- **A2 — sibling aggregation math:** hand-computed composite for a hypothetical claudit/skillet/commventional-installed scenario (parser outputs 80/70/85 + presence-cap/fails for the rest) = 59.75 rounded to 60/C. Matches the orchestrator's Phase 5 formula exactly; JSON round-trips because the shape uses only native types.
- **A3 — graceful degradation:** same fixture as A1 with explicit per-dim annotation check — every presence-fail row carries `recommended: <plugin>` + install command; every kernel-presence-cap row same; kernel-owned rows reference kernel-check findings. No unhandled `source` enum values.

Dry-run is honest about what was verified: arithmetic + logic + Bash scans. Live behavior — AskUserQuestion flows, parser-subagent dispatches, scorecard rendering in a terminal — is deferred to Alfred's review environment. Tickets a1/a2/a3 record the scope explicitly.

## 22:45 — PR opened

PR #42: https://github.com/acostanzo/quickstop/pull/42 — `feat(pronto): Phase 1 — kernel + rubric + audit orchestrator`. 14 commits, each atomic-and-conventional; merge strategy is rebase-and-merge (don't squash). Title matches the plan's DoD-specified string. Body covers what landed, what's verified here, what's deferred to live review, and a test-plan checklist for the three A-bars plus the plugin-load smoke test and the two idle skills.

DoD checks all passing:
- 12/12 T-tickets committed, atomic + conventional ✓
- 3/3 A-bars dry-run validated (Bash scans + hand-arithmetic + logic verification); live pending ✓ (scoped in tickets)
- README model-explanation section 199 words (under 200 DoD threshold) ✓
- Zero author-string matches inside `plugins/pronto/` ✓
- plugin.json valid JSON; check-plugin-versions.sh passes (new plugin v0.1.0) ✓
- ADR 001 landed as accepted ✓
- Plugin tree loads cleanly (structure verified; live `/reload-plugins` is part of the operator's smoke test) ✓

Handing off to Alfred for review.














## 18:53

T1 landed — `plugins/avanti/` scaffolded with `plugin.json` (declaring the pronto `project-record` audit wire contract natively), stub README, marketplace registration, and root README entry. JSON validates; ticket record filed in `project/tickets/closed/`.

## 19:00

T2 landed — `references/sdlc-conventions.md` is the canonical reference for the lifecycle model. Covers state machines for plans/tickets/ADRs, folder-as-primary, frontmatter schemas, per-day pulse structure, promote semantics, pitfalls. 231 lines; README already links to it.
