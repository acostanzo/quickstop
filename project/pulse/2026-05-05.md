# Pulse — 2026-05-05


## 15:21

Authored inkwell-expansion plan, 5 tickets (t1-t5), and ADR for corroboration architecture. Ready for M1 dispatch.

## 15:46

Promoted adr 007-inkwell-corroboration-architecture: proposed → accepted.

## 15:51

T1 shipped: Diátaxis templates landed, document model + voice published in README, parse-inkwell removed.

## 15:58

Promoted ticket t1-m1-document-model: open → closed.

## 16:08

T2 shipped: /inkwell:doc and /inkwell:search wired, FTS5 index machinery in place, suggest-links seeded.

## 16:24

Promoted ticket t2-m2-doc-and-search: open → closed.

## 16:42

T3 shipped: /inkwell:query lands retrieval + citation contract +
stubbed corroboration. Contract surface locked at M3 so M5 can wire
the corroboration verdict without reshaping. Deterministic half is
bin/inkwell-query-retrieve.sh; LLM half is the skill body's
synthesis paragraph.

## 16:45

Promoted ticket t3-m3-query: open → closed.

## 16:50

T4 shipped: /inkwell:tidy lands the maintenance surface — read-only by
default with five rules (duplicate, dead-link, stale,
template-non-compliance, missing-related), --apply for mechanical
fixes (link rewrites on git renames, frontmatter `updated:` bumps,
archive-stale to docs/archive/, near-identical dedup), and
--apply-semantic for diff-only dedup-choice proposals in the
0.85..0.95 overlap band. Thresholds DRY'd into
references/thresholds.json; the staleness scorer reads from the same
source so tidy and the audit can never drift apart.

## 17:05

Promoted ticket t4-m4-tidy: open → closed.

## 18:30

T5 shipped. Inference-time corroboration layer wired into
`/inkwell:query` (Tiers 1–3): deterministic name-resolution via
grep, subagent-judged behavioural verification via
`claude -p --model haiku` with bounded timeout, and a Tier 3
"could not corroborate" annotation for conceptual content.
Graceful degradation pinned by test — subagent unreachable returns
`could not corroborate`, the response always ships. Three
conditional audit scorers landed:
`score-template-compliance.sh`, `score-backlink-coverage.sh`,
`score-duplicate-density.sh`, all gated on inkwell frontmatter and
empty-scoping on non-inkwell consumers. A4 regression test is the
load-bearing assertion; the four pre-T5 scorers are byte-equivalent
across marked and plain fixtures.

Promoted ticket t5-m5-corroboration-and-audit-scorers: open →
closed. Plan inkwell-expansion: active → done. M1–M5 closed; the
release PR (version bump + marketplace + README rewrite) follows
separately.
